--- a/opt/victronenergy/vesmart-server/vesmart_server.py
+++ b/opt/victronenergy/vesmart-server/vesmart_server.py
@@ -56,6 +56,7 @@
 	logger.info("device disconnected: %s", device_path)
 	assert device_path in _connected
 	_connected.remove(device_path)
+	Vesmartserver.device_disconnected(device_path)
 	if len(_connected) == 0:
 		LedState.disconnected()
 		Advertisement.disconnected()
